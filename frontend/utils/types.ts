export type CourseTypes = {
    id: number;
    title: string;
    ownerId: number;
    progress: number;
};

export type ProjectTypes = {
  id: number;
  name: string;
  status: string;
  ownerId: number;
}